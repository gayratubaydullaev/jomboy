# Copy to .env.test.local for local unit tests (optional).
# Unit tests (vitest) do not require a full Next.js production build.

NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
JWT_SECRET=dev-test-jwt-secret-min-32-characters!!
SESSION_COOKIE_SECRET=dev-test-session-secret-min-32-chars!!
